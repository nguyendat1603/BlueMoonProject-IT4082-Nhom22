spring:
  application:
    name: HomeTechManagement

  # PostgreSQL Configuration
  datasource:
    url: jdbc:postgresql://ep-late-shape-ahcz0zed-pooler.c-3.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_6D5MgcNqwCvl&sslmode=require&channelBinding=require
    username: neondb_owner
    password: npg_6D5MgcNqwCvl
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 60000
      idle-timeout: 300000
      max-lifetime: 1800000
      leak-detection-threshold: 10000

  # JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: update # Tự động tạo/điều chỉnh bảng (development). Production: use migrations.
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

  mail:
    host: smtp.gmail.com           # Thay bằng host của dịch vụ email
    port: 587
    username: bluemoonnoreply@gmail.com # Thay bằng email gửi
    password: ahylnpruyfjuddjh                        #bluemoon20242
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Logging Configuration (merged with debug levels)
logging:
  level:
    root: ERROR
    io.github.ktpm.bluemoonmanagement.startup.DataLoader: INFO
    org.springframework.web.socket: DEBUG
    org.springframework.messaging: DEBUG
  pattern:
    console: "%d{HH:mm:ss} %-5p %c{1} - %m%n"
